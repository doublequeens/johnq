---
const base = import.meta.env.BASE_URL;
const navItems = [
  { name: 'Home', href: `${base}` },
  { name: 'Technical', href: `${base}technical/` },
  { name: 'Life', href: `${base}life/` },
  { name: 'About', href: `${base}about/` }
];
---

<header class="header">
  <div class="bar">
    <a href={base} class="brand">John Q</a>

    <nav class="nav" aria-label="Main navigation">
      {navItems.map((item) => (
        <a href={item.href} class="nav-link">{item.name}</a>
      ))}
    </nav>

    <div class="right-spacer" aria-hidden="true">John Q</div>
  </div>
</header>

<style>
  .header {
    padding: 18px 0 14px;
    border-bottom: 1px solid #e8e6e1;
  }

  .bar {
    width: min(1280px, calc(100vw - 64px));
    margin-left: 50%;
    transform: translateX(-50%);
    display: grid;
    grid-template-columns: auto 1fr auto;
    align-items: center;
    gap: 18px;
  }

  .brand,
  .right-spacer {
    font-size: 35px;
    line-height: 1;
    letter-spacing: -0.025em;
    font-weight: 400;
    white-space: nowrap;
  }

  .brand {
    color: #181818;
    text-decoration: none;
  }

  .brand:hover { color: #101010; }

  .right-spacer {
    visibility: hidden;
    pointer-events: none;
    user-select: none;
  }

  .nav {
    display: flex;
    justify-content: center;
    align-items: center;
    gap: 30px;
  }

  .nav-link {
    font-size: 20px;
    color: #4b4b4b;
    text-decoration: none;
    font-weight: 400;
    letter-spacing: -0.01em;
    line-height: 1;
  }

  .nav-link:hover { color: #111; }

  @media (max-width: 1180px) {
    .bar {
      width: min(1020px, calc(100vw - 36px));
    }

    .brand,
    .right-spacer {
      font-size: 42px;
    }

    .nav-link {
      font-size: 20px;
    }
  }

  @media (max-width: 760px) {
    .bar {
      width: calc(100vw - 24px);
      grid-template-columns: 1fr;
      gap: 12px;
    }

    .brand {
      font-size: 34px;
    }

    .nav {
      justify-content: flex-start;
      flex-wrap: wrap;
      gap: 14px;
    }

    .nav-link {
      font-size: 24px;
    }

    .right-spacer { display: none; }
  }
</style>
